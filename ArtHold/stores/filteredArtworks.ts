/*import { defineStore } from "pinia";
import type { Artwork } from "@prisma/client";

export const useFilteredArtworksStore = defineStore("filteredArtworks", {
  state: () => ({
    artworks: [] as Artwork[],
  }),
  actions: {
    setArtworks(artworks: Artwork[]) {
      this.artworks = artworks;
    },
  },
});
*/
